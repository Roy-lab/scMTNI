This program will output the giant component of the graph and while it is doing that it will suppress nodes which don't have enough neighbors (min_hub_size).

Usage:
./getComponents inputnetworkfile outputsuff min_hub_size
